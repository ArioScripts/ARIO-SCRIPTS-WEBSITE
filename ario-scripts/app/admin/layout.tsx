import { requireAdmin } from "@/lib/auth"; import Link from "next/link";
export default async function AdminLayout({children}:{children:React.ReactNode}){await requireAdmin();return <div className="mx-auto flex max-w-7xl gap-6 px-5 py-8"><aside className="hidden w-56 shrink-0 space-y-2 md:block">{["Dashboard","Scripts","Categories","Users","Reports","Logs","Settings"].map(x=><Link className="block rounded-xl px-4 py-3 text-sm text-white/60 hover:bg-white/5" key={x} href={`/admin/${x==="Dashboard"?"":x.toLowerCase()}`}>{x}</Link>)}</aside><section className="min-w-0 flex-1">{children}</section></div>}
