import { supabaseServer } from "@/lib/supabase/server";
export default async function Categories(){const db=await supabaseServer();const {data}=await db.from("categories").select("id,name,slug,description").order("name");return <main className="mx-auto max-w-7xl px-5 py-16"><h1 className="text-4xl font-black">Categories</h1><div className="mt-8 grid gap-4 md:grid-cols-3">{(data||[]).map(c=><a href={`/scripts?category=${c.slug}`} key={c.id} className="card-hover glass rounded-2xl p-6"><h2 className="font-bold">{c.name}</h2><p className="mt-2 text-sm text-white/45">{c.description}</p></a>)}</div></main>}
