'use client'; import { useState } from "react"; import { createBrowserClient } from "@supabase/ssr"; import { useRouter } from "next/navigation";
export default function Register(){const [username,setU]=useState(""),[email,setE]=useState(""),[password,setP]=useState(""),[error,setError]=useState("");const r=useRouter();const db=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);async function submit(e:React.FormEvent){e.preventDefault();const {error}=await db.auth.signUp({email,password,options:{data:{username}}});if(error)setError(error.message);else r.push("/login");}return <main className="mx-auto max-w-md px-5 py-20"><h1 className="text-4xl font-black">Create account</h1><form onSubmit={submit} className="glass mt-8 space-y-4 rounded-2xl p-6"><input required placeholder="Username" value={username} onChange={e=>setU(e.target.value)} className="w-full rounded-xl bg-white/5 p-3"/><input required type="email" placeholder="Email" value={email} onChange={e=>setE(e.target.value)} className="w-full rounded-xl bg-white/5 p-3"/><input required minLength={8} type="password" placeholder="Password" value={password} onChange={e=>setP(e.target.value)} className="w-full rounded-xl bg-white/5 p-3"/>{error&&<p className="text-sm text-red-400">{error}</p>}<button className="w-full rounded-xl bg-white p-3 font-semibold text-black">Register</button></form></main>}
